{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Bienvenue sur mon site de présentation personnelle</h1>
        <p>Explorez mes compétences, mon CV, mes projets, et bien plus encore.</p>

        <div class="row mt-4">
            <div class="col-md-4">
                <a href="{{ path('app_qui_suis_je') }}" class="btn btn-primary btn-block">
                    Qui suis-je ?
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_cv_index') }}" class="btn btn-success btn-block">
                    Mon CV
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_project_index') }}" class="btn btn-info btn-block">
                    Mon Projet de Terminale
                </a>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-md-4">
                <a href="{{ path('app_competence_index') }}" class="btn btn-warning btn-block">
                    Portefeuille de Caca
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_technology_index') }}" class="btn btn-danger btn-block">
                    Veille Technologique
                </a>
            </div>
        </div>
    </div>
{% endblock %}
